# Requirements — Business Insights & Sales Forecasting Tool
# Complete list of dependencies for the entire project (FastAPI, React, Prophet, DNN, RAG)
# Install with: pip install -r requirements.txt

# ── Core & Data Manipulation ─────────────────────────────
numpy>=1.24
pandas>=2.0
openpyxl>=3.1            # For reading Excel datasets
PyYAML>=6.0              # For config files
python-dotenv>=1.0       # For .env loading
requests>=2.31           # For general HTTP requests

# ── Database (PostgreSQL) ────────────────────────────────
psycopg2-binary>=2.9     # PostgreSQL adapter for ETL
sqlalchemy>=2.0          # ORM & Database toolkit
psycopg[binary]>=2.9     # SQLAlchemy PostgreSQL support

# ── Machine Learning (Preprocessing & Baselines) ─────────
scikit-learn>=1.3
imbalanced-learn>=0.11   # Provides 'imblearn' (SMOTE)
joblib>=1.3              # Model saving/loading

# ── Deep Learning (Customer Churn) ───────────────────────
tensorflow>=2.13         # DNN for Churn Prediction

# ── Visualisation ────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12

# ── Time-Series Forecasting ──────────────────────────────
prophet>=1.1             # Prophet model
holidays>=0.25           # Required by Prophet for UK holidays

# ── Agentic RAG Chatbot (LangGraph & Groq) ───────────────
langchain>=0.1
langchain-community>=0.0.20
langchain-core>=0.1
langgraph>=0.1
langchain-groq>=0.1
groq>=0.4                # Official Groq Python SDK
chromadb>=0.4            # Local Vector Database
sentence-transformers>=2.2 # Local Embeddings (HuggingFace)
diskcache>=5.6           # Caching tool outputs

# ── API Backend ──────────────────────────────────────────
fastapi>=0.111           # Web framework
uvicorn[standard]>=0.30  # ASGI server (includes httpx, websockets)
pydantic>=2.0            # Data validation

# ── Optional (LSTM Experiments) ──────────────────────────
torch>=2.0
